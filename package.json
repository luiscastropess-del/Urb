{
  "name": "urb-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,admin,guia -c blue,red,green \"pnpm --filter @urb/web dev\" \"pnpm --filter @urb/admin dev\" \"pnpm --filter @urb/guia dev\"",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0"
  }
}
